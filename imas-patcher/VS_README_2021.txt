To use the .sln file you must have the following installed:

- Visual Studio 2019
- Qt 5.15.2
- Qt VS Tools extension

Static Linking:
To make static builds of the imas-patcher, add the -static switch to the configure command when compiling Qt from sources.